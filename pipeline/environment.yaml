name: pipeline
channels:
  - bioconda
  - conda-forge
dependencies:
  - pandas
  - ete3
  - snakemake-minimal
  - ipykernel
  - fastcluster
  - matplotlib
  - seaborn
  - scikit-learn
  - jinja2
  
# to update "conda env update --name pipeline --file environment.yaml"